<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="mLocationData"
            type="com.taximobility.bookingmodule.LocationData"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_marginTop="@dimen/dp_4"
        android:id="@+id/mainLocLay"
        android:paddingTop="@dimen/dp_5"
        android:paddingBottom="@dimen/dp_5"
        android:descendantFocusability="blocksDescendants"
        android:layout_height="wrap_content">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/imgLocType"
            android:layout_width="wrap_content"
            android:padding="@dimen/dp_10"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dp_10"
            android:src="@drawable/arrrowright"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/txt_loc_header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@+id/imgLocType"
            tools:text="Location"
            android:layout_marginStart="@dimen/dp_8"
            android:textColor="@color/black"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{mLocationData.label_name}"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/txt_loc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@{mLocationData.location_name}"
            android:layout_marginStart="@dimen/dp_8"
            tools:text="Location"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/imgLocType"
            app:layout_constraintEnd_toStartOf="@+id/imgLocDelete"
            app:layout_constraintTop_toBottomOf="@+id/txt_loc_header" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/imgLocDelete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/dp_3"
            android:padding="@dimen/dp_15"
            android:src="@drawable/delete"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
             />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>